declare type Headers = {
    [id: string]: string;
};
interface Response {
    status: any;
    headers: Headers;
    body: any;
}
/**
 * Provides generic network interface
 */
declare class Transport {
    constructor();
    private static request(method, url, headers, body?);
    /**
     * Make a GET request by given URL
     */
    get(url: string, headers: Headers): Promise<Response>;
    /**
     * Make a POST request by given URL
     */
    post(url: string, headers: Headers, body?: string): Promise<Response>;
    /**
     * Make a PUT request by given URL
     */
    put(url: string, headers: Headers, body: any): Promise<Response>;
    /**
     * Make a DELETE request by given URL
     */
    delete(url: string, headers: Headers): Promise<Response>;
}
export { Headers, Response, Transport };
