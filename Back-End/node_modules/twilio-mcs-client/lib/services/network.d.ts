/// <reference types="node" />
import { Configuration } from '../configuration';
import * as FormData from 'isomorphic-form-data';
import { Transport } from 'twilio-transport';
declare class Network {
    private readonly config;
    private readonly transport;
    constructor(config: Configuration, transport: Transport);
    private backoffConfig;
    private retryWhenThrottled;
    private executeWithRetry;
    get(url: string): Promise<any>;
    post(url: string, media: string | Buffer | FormData, contentType?: string): Promise<any>;
}
export { Network };
